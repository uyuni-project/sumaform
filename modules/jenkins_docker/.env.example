# ── Jenkins Docker Deployment — Environment Variables ─────────────────────
# Copy this file to .env and fill in your values before running docker compose.
#
#   cp .env.example .env
#   chmod 600 .env        # keep secrets off other users
#
# NEVER commit .env to version control (it is in .gitignore).
# ──────────────────────────────────────────────────────────────────────────

# ── Jenkins admin credentials ─────────────────────────────────────────────
JENKINS_ADMIN_ID=user
JENKINS_ADMIN_PASSWORD=password

# Name of the API token that will be auto-generated on first boot
JENKINS_API_TOKEN_NAME=automation-token

# Public URL for Jenkins (used in email notifications, build URLs, etc.)
JENKINS_URL_PUBLIC=https://jenkins-controller.mgr.suse.de

# ── Agent configuration ───────────────────────────────────────────────────
# This must match the node name registered in 02-setup-agent.groovy
JENKINS_AGENT_NAME=opensuse-agent

# AGENT_SECRET is printed to the Jenkins controller log on first boot:
#
#   docker logs jenkins-controller 2>&1 | grep "AGENT SECRET"
#
# Paste the value below, then restart the worker:
#
#   docker compose restart opensuse-worker
#
AGENT_SECRET=REPLACE_ME_AFTER_FIRST_BOOT
