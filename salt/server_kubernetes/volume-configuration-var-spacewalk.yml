{% set storage_class = grains.get('kubernetes_storage_class') or 'local-path' %}
{% set local_path = grains.get('local_path_provisioner_path') or '/opt/local-path-provisioner' %}
{% set var_spacewalk_host_path = grains.get('kubernetes_var_spacewalk_host_path') or local_path ~ '/var-spacewalk' %}
apiVersion: v1
kind: PersistentVolume
metadata:
  name: var-spacewalk
  annotations:
    pv.kubernetes.io/provisioned-by: rancher.io/local-path
spec:
  capacity:
    storage: 100Gi
  accessModes:
    - ReadWriteOnce
{% if storage_class == '-' %}
  storageClassName: ""
{% else %}
  storageClassName: "{{ storage_class }}"
{% endif %}

  claimRef:
    namespace: uyuni
    name: var-spacewalk
  hostPath:
    path: "{{ var_spacewalk_host_path }}"
    type: DirectoryOrCreate
  persistentVolumeReclaimPolicy: Delete
  volumeMode: Filesystem
