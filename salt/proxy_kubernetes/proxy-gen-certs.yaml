apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ proxy_name}}
  namespace: {{ proxy_namespace }}
  labels:
    app.kubernetes.io/part-of: uyuni
spec:
  secretName: {{ proxy_name}}
  secretTemplate:
    labels:
      app.kubernetes.io/part-of: uyuni
  isCA: false
  usages:
  - server auth
  privateKey:
    algorithm: RSA
    encoding: PKCS1
    size: 2048
    rotationPolicy: Always
  dnsNames:
    - {{ proxy_FQDN }}
  commonName: {{ proxy_FQDN }}
  issuerRef:
    name: "uyuni-issuer"
    kind: ClusterIssuer
    group: cert-manager.io
